#!/usr/bin/env bash
# 🧮 Physics Lab Equation Repair CLI Shortcut: fixlatex
# Usage:
#   scripts/fixlatex "<URL|ID|LaTeX>"
#   scripts/fixlatex --dry-run "<URL>"
#   scripts/fixlatex --json "<URL>"

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
php "${SCRIPT_DIR}/fix_equation_by_url.php" "$@"
