#!/usr/bin/env bash
# 🌌 Physics Lab Formula Lineage & Derivation Diagnostic CLI: fixlineage
# Usage:
#   scripts/fixlineage "<URL|ID|LaTeX|Keyword>"
#   scripts/fixlineage --summary
#   scripts/fixlineage --thin-top 20
#   scripts/fixlineage --search "Bernoulli"
#   scripts/fixlineage --heal

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
python3 "${SCRIPT_DIR}/maintenance/audit_lineage_health.py" "$@"
